购物车：
	登录成功后直接跳转到order.html界面

day01：
	数据库的数据库的设计及创建、实体类的创建、(不确定能不能用到该功能)图片操作的工具（图片转数组）	
遇到的问题以及解决办法：
	a、序列的创建及使用
		①、获取序列的当前值
		select 序列名 from 表名
		②、插入数据的时候
		设置了序列的字段插入值的时候写 序列名.nextval就OK了！
		③、获取序列的当前值（当前插入值的序列）
		select 序列名.currval from 表名	
	b、外键的设置及使用
		a、 consetraint 自定义错误信息 foreign key （字段一）references 表名（字段二）；
dya02：
	登录注册功能
需要注意的问题：
	a、导入jar包后要fix
	b、不会自己书写C3p0Utils
	c、前端HTML标签里面的name值一定要和服务端getParameter（）里面的参数相同
注册成功后倒计时跳转登陆界面未完成；
	
day03:
	
	a、登录注册功能全部完成（注册时用户名的验证以及密码检验除外）
	b、还需要理清整个项目的条理
	c、当用户成功登录的时候界面的登录字样替换为用户名；
	
	d、商品添加到购物车--向服务端发送请求location.href;
		问题：location.href发送的.do请求浏览器界面会出现下载请求？
		解决：还没有解决
	遇到的问题：
		在数据库插入数据的时候不小心插入换行符不容易被发现。
	目前在做登录成功添加购物车；
		初步猜测：每当注册一个用户就应当有一个对应的空购物车。
		
	添加商品到购物车功能完成。
		下一步查询商品总量，以及总价钱展示在购物车界面中
	问题：group by 语句使用不熟练
		select 后面跟的字段名要么是group by后面跟的字段名要么是被聚合函数包含的字段；
day04:
	最大问题：
		用户编号传不到后台；
	要特别注意的问题：
		数据库的设计和实体类创建时数据类型必须保持一致，一旦不一致很难检查出来。
	JSP页面之间通信
		1、url传值
			优点：简单 支持平台多样性
			缺点：保密性不行，只能传字符串数据类型
		2、表单传值
		3、cookie传值
		4、session传值
	总结：购物车展示基本完成，明天修改购物车里面的数据以及清空购物车
day05：
	不刷新返回上一页
	οnclick="location.href='javascript:history.go(-1);'" 
	返回父路径下的文件
	在前面加上../
	页面通讯只能发送String类型的值
	
		
	